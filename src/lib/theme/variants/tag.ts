import { radii, space } from '../design-system';

export const tag = {
  all: 'unset',
  gap: space.s,
  display: 'flex',
  cursor: 'pointer',
  alignItems: 'center',
  borderRadius: radii.full,
  transition: 'all 300ms ease-in-out',
};
